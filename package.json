{
  "name": "nano-spa",
  "version": "1.0.0",
  "main": "index.js",
  "author": "5alidz <kzack-dev@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rollup --config && uglifyjs --compress --mangle -o dist/bundle.min.js -- dist/bundle.js && ls -la dist",
    "dev": "cd ./dev && serve -l 3000 --single --no-clipboard"
  }
}
