{
  "name": "nano_spa_src",
  "version": "1.0.0",
  "main": "index.js",
  "author": "5alidz <kzack-dev@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "build-src": "rollup --config && uglifyjs --compress --mangle -o dist/bundle.min.js -- dist/bundle.js && echo 'module.exports=nano_spa' >> dist/bundle.min.js && ls -la dist && git add . && git commit -m 'new-build'",
    "dev": "cd ./dev && yarn start"
  }
}
